# edge-lora-test — version the RECIPES (scripts/configs/plans/data), NOT the
# regenerable heavy artifacts (model weights, adapters, downloaded models).

# --- trained adapters (regenerable from configs + data) ---
adapters*/

# --- downloaded / converted models ---
LFM2.5-*/
qiyas-edge-*/
draft_src/
*.safetensors
*.gguf
*.bin
*.npz
*.npy
*.pt
*.pth

# --- downloadable assets ---
lid.176.ftz

# --- generated hobby packs (regenerable via hobby_pack.refresh) ---
packs/

# --- runtime state (mutated each run; seeded by tests/use, not a recipe) ---
nudges.json
preferences.json
notes.json
reminders.json
mp_store.json
mp_docs.bin
mp_facts.json
e5_mlx_vectors.json

# --- python env / caches ---
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
